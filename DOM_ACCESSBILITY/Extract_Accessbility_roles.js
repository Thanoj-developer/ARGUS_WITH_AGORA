/**
 * DOM_ACCESSBILITY/Extract_Accessbility_roles.js
 * Step 2: Extract & Filter Accessibility Roles
 */

const { ACTIONABLE_ROLES, extractInteractive, extractAccessibilityRoles } = require('./accessibility');

module.exports = {
  ACTIONABLE_ROLES,
  extractInteractive,
  extractAccessibilityRoles
};
